{
  "name": "face-ai-guidance",
  "version": "1.0.0",
  "description": "AI Guidance for Global Health Professionals – FACE/Task Force for Global Health",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy && npx pagefind --site _site",
    "build:eleventy": "npx @11ty/eleventy",
    "build:search": "npx pagefind --site _site"
  },
  "keywords": ["ai", "global-health", "guidance", "eleventy"],
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "pagefind": "^1.3.0"
  }
}
